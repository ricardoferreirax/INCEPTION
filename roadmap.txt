VM Setup

	Hostname: rmedeiro42
	Domain name: empty
	Root password: root
	Full Name: Ricardo Medeiros
	Username: rmedeiro
	Password: 1234
	Partitions: Guided - use entire disk
	Partition scheme: All file in one partition
	Finish partitioning and write changes to disk
	Write the changes to disk? Yes
	Scan extra installation media? Yes. And it will show another answer box and click No
	Debian archive mirror: deb.debian.org
	Choose software to install: [*] Debian desktop environment
								[*] SSH server
								[*] standard system utilities	
	Install GRUB bootloader: Yes, and install in /dev/sda
	FInish the installation: Continue

==========================================================================================================================================================================

Debian Setup

	1) Install sudo: 
	
	   a) Switch to the root user:  su -

	   b) Update and upgrade the installed packages:  apt-get update && apt-get upgrade -y

	   c) Installs the sudo package, allowing users to execute commands with admnistrative privileges:  
	      apt-get install sudo -y

    2) Add User to the Sudo Group: 
	
	   a) Add user to the sudo group:  adduser rmedeiro sudo

	      Grants admnistrative privileges to the user through Debian sudo group.
	
	   b) Check that the user belongs to the sudo group:   getent group sudo

	   c) Apply the change rebooting the system:  sudo reboot

	3) Update system:  sudo apt update && sudo apt upgrade -y

	4) Install Vim: sudo apt install -y vim

	5) Go to sudoers file: sudo visudo
	   Edit the line below the root line adding username and the privileges: rmedeiro ALL=(ALL:ALL) ALL

	6) Install OpenSSH Server: 
	
	   a) Install OpenSSH server:  sudo apt install -y openssh-server

	      Allows remote connections to the virtual machine using SSH

	   b) Enable SSH at boot:  sudo systemctl enable ssh

	   c) Start the SSH service:  sudo systemctl start ssh

	   d) Check that SSH is running:  sudo systemctl status ssh

	      Expected status: active (running).
     
	7) Change SSH port 22 to 4242: 
	
	   a) Open the SSH configuration file:  sudo vim /etc/ssh/sshd_config

	   b) Find: #Port 22 and replace with Port 4242. Save and exit.

	      Changes the SSH listenning port 22 to 4242.

	   c) Restart SSH:  sudo systemctl restart ssh

	   d) Check:  sudo systemctl status ssh (check)

	8) Configure VS COde Remote SSH:  

	   a) Install the Remote - SSH extension in VS Code.

       b) Connect to the VM: Open VS Code and press Ctrl + Shift + P .

	   c) Select: Remote-SSH: Connect to Host... and choose Add New SSH Host...

	   d) Enter: ssh -p 4242 rmedeiro@<VM_IP>  (Example: ssh -p 4242 rmedeiro@192.168.1.120)

          To obtain the VM IP: hostname -I .

	   e) Select the SSH config file, connect to the host and enter the VM password when prompted.

    9) Install aplications: sudo apt install -y git curl wget tree zsh make gcc build-essential ca-certificates gnupg lsb-release apt-transport-https
                            chsh -s $(which zsh)  (default zsh)
                            OhMyZsh: sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"


==========================================================================================================================================================================

Docker Engine installation

	> Update System:  sudo apt-get update && sudo apt-get upgrade -y

	> Install Required Depedencies:  sudo apt-get install ca-certificates curl gnupg -y

      Installs the packages required to securely download and verify Docker packages:  -> ca-certificates: HTTPS certificate validation.
																					   -> curl: Download files from the internet.
																					   -> gnupg: Verify package signatures using GPG keys.

	> Create Keyrings Directory:  sudo install -m 0755 -d /etc/apt/keyrings

      Creates the directory where trusted repository keys will be stored.

	> Add Docker Official GPG key:  curl -fsSL https://download.docker.com/linux/debian/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

      Downloads Docker's official signing key and converts it to the binary GPG format required by APT.

	> Set Key Permissions:  sudo chmod a+r /etc/apt/keyrings/docker.gpg

	  Allows the APT package manager to read the Docker GPG key.

	> Add Docker repository:  echo "deb [arch="$(dpkg --print-architecture)" signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian \
							  "$(. /etc/os-release && echo "$VERSION_CODENAME")" stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

	  Adds Docker's official repository to the APT sources list.

	> Update packages: sudo apt-get update

	> Check Docker Repository:  apt-cache policy docker-ce

	  Checks that Docker packages will be downloaded from Docker's official repository instead of the default Debian repositories.

	> Install Docker Engine:  sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

      Installs: -> docker-ce: Docker Engine.
				-> docker-ce-cli: Docker command-line interface.
				-> containerd.io: Container runtime.
				-> docker-buildx-plugin: Advanced image building support.
				-> docker-compose-plugin: Docker Compose v2 (docker compose).

	> Start Docker Service:  sudo systemctl start docker

	  Starts the Docker daemon immediately.

	> Enable Docker at Boot:  sudo systemctl enable docker

	  Ensures Docker starts automatically every time the system boots.

	> Add User to Docker Group:  sudo gpasswd -a $USER docker

	  Allows the current user to run Docker commands without using sudo.

	> Apply Group Changes: newgrp docker

	  Applies the new group membership without requiring a logout or reboot.

	> Check Group Membership:  groups

	  Confirms that the current user belongs to the Docker group.
	  Expected output:  rmedeiro sudo docker

	> Check Docker Installation:  docker run hello-world

	  Downloads and runs Docker's test container. Expected output: Hello from Docker!
      If this message appears, Docker is installed correctly.

	> Check Docker Version:  docker --version

	> Check Docker Compose:  docker compose version

      Checks that Docker Compose v2 is installed and available.

	> Check Docker Service Status:  systemctl status docker

	  Displays the current state of the Docker daemon.
      Expected status: active (running)

    > Display Docker Information: docker info

	  Displays detailed information about: - Docker version
										   - Storage driver
										   - Available networks
										   - Available volumes
										   - Docker root directory
										   - Running containers

	  This command is useful to confirm that the Docker daemon is fully operational.

==========================================================================================================================================================================

Configure the Local Domain (username.42.fr)

The Inception subject requires the website to be accessible through: user.42.fr  (e.g: rmedeiro.42.fr)

However, this domain does not exist on the public internet. To make the operating system recognize it, we must 
manually create a local DNS entry using the /etc/hosts file.

What is /etc/hosts ? The /etc/hosts file is a local DNS table.
Before contacting an external DNS server, Linux checks this file to see if the domain name is already known.

Example:   rmedeiro.42.fr
                 |
				 v
             127.0.0.1

1) Edit the Hosts File - open the hosts file:  sudo vim /etc/hosts

   We will see something similar to:  127.0.0.1 localhost
                                      127.0.1.1 debian

2) Add 42 Domain - add the following line:  127.0.0.1 rmedeiro.42.fr
   
   The file should look similar to:  127.0.0.1 localhost
                                     127.0.0.1 rmedeiro.42.fr
                                     127.0.1.1 debian

3) Check the Configuration:  ping rmedeiro.42.fr

   Expected output: PING rmedeiro.42.fr (127.0.0.1)


How Does It Work? When the browser requests https://rmedeiro.42.fr , Linux performs:  rmedeiro.42.fr
																						   |
																						   v
																					   127.0.0.1
																					       |
																						   v
																					 NGINX Container
																					       |
																						   v
																						WordPress
																						   |
																						   v
																						MariaDB

No public DNS server is involved. Everything happens locally inside the VM.

The /etc/hosts file acts as a local DNS table.
When the browser requests https://rmedeiro.42.fr the operating system automatically translates: rmedeiro.42.fr → 127.0.0.1
which points to the local machine.
This allows NGINX to receive requests for the domain required by the subject without needing a real public DNS record.

If you access the website from your host machine (outside the VM), you must also add 127.0.0.1 rmedeiro.42.fr to the host 
machine's hosts file.
Otherwise, the browser running on the host will not be able to resolve the domain name.

==========================================================================================================================================================================

Create the Inception's Project Structure

Before writing any Dockerfile, we must create the directory structure required by the subject.
Inside Vm, we create the directories: mkdir -p secrets
									  mkdir -p srcs/requirements/mariadb/conf
									  mkdir -p srcs/requirements/mariadb/tools
									  mkdir -p srcs/requirements/wordpress/conf
									  mkdir -p srcs/requirements/wordpress/tools
									  mkdir -p srcs/requirements/nginx/conf
									  mkdir -p srcs/requirements/nginx/tools

We have something like that:

									inception/ 
									├── Makefile 
									├── .gitignore 
									├── secrets/ 
									│ 
									└── srcs/ 
									    ├── .env 
										├── docker-compose.yml 
										│ 
										└── requirements/ 
										    │ 
											├── mariadb/ 
											│   ├── Dockerfile 
											│   ├── conf/ 
											│   └── tools/ 
											│ 
											├── wordpress/ 
											│   ├── Dockerfile 
											│   ├── conf/ 
											│   └── tools/ 
											│ 
											└── nginx/ 
											    ├── Dockerfile 
												├── conf/ 
												└── tools/


Recommended Development Order

Do NOT start by writing everything at once. Build the infrastructure layer by layer.

Step 1 - Create Volumes.
Step 2 - Create .env.
Step 3 - Create Docker Secrets.
Step 4 - Create .gitignore.
Step 5 - Create docker-compose.yml.
         Here we start by defining network, volumes and mariadb service. Do not worry about NGINX or WP yet.
Step 6 - Build MariaDB.
         Create: requirements/mariadb/Dockerfile
                 requirements/mariadb/conf/*
                 requirements/mariadb/tools/*
         Goals: container starts
                database is created
                user is created
                volume persists data
         Test MariaDB until it works perfectly.
Step 7 - Build WordPress + PHP-FPM.
         Create: requirements/wordpress/Dockerfile
                 requirements/wordpress/tools/*
         Goals: PHP-FPM starts
                WordPress installs automatically
                connection to MariaDB works
         Do not start NGINX yet.
Step 8 - Build NGINX.
         Create: requirements/nginx/Dockerfile
                 requirements/nginx/conf/*
                 requirements/nginx/tools/*
         Goals: TLSv1.2/TLSv1.3 only
                self-signed certificate
                proxy PHP requests to WordPress
Step 9 - Connect Everything.
         Check if NGINX, WordPress and MariaDB comunicate through the Docker network.
Step 10 - Create the Makefile.
Step 11 - Final Testing.
          Verify: docker ps
                  docker images
                  docker volume ls
                  docker network ls
          Open https://rmedeiro.42.fr.
          Confirm: TLS works
                   WordPress loads
                   Database persists after container recreation
                   Containers restart correctly
                   No passwords exist inside the repository

==========================================================================================================================================================================
											
Create Volumes on the Host Machine

One of the requirements of the Inception subject is to use Docker named volumes for persistent storage.
The subject explicitly states that the volume data must be stored inside /home/login/data , where login is our 42 login.

Where Should These Directories Be Created? Although we are running Debian inside VirtualBox, the Debian VM is considered 
the Docker host machine because Docker Engine is installed and running inside the VM. Therefore, when the subject says 
/home/login/data on the host machine it means inside the Debian virtual machine. Docker never sees our physical computer. 
Docker only sees the Debian VM where it is installed.

Create the folders:  mkdir -p /home/rmedeiro/data/mariadb
                     mkdir -p /home/rmedeiro/data/wordpress

Check that they were created successfully:  ls -la /home/rmedeiro/data

Why Do We Need These Directories? Containers are designed to be temporary. If a container is remover, for example,
docker rm mariadb, it completely removes the container and its internal filesystem.
Without persistent storage, MariaDB database, WordPress files, uploaded content, users and posts would be lost.
So, MariaDB and WordPress need persistent data. With volumes the data is preserved.
The containers can be destroyed and recreated while the date remains safely stored inside /home/rmedeiro/data/mariadb
and /home/rmedeiro/data/wordpress .
This way, if containers are deleted and recreated, the data is not lost.
This is exactly why the Inception subject requires persistent volumes.

==========================================================================================================================================================================

Create the .env file

The .env file stores environment variables used by Docker Compose and by the other containers, such as MariaDB, WordPres 
and NGINX.

Instead of hardcoding configuration values directly inside docker-compose.yml, Dockerfiles, shell scripts or nginx.conf, we 
define them once in .env file and reuse them throughout the project.
This makes the project easier to maintain, to configure and safer from accidental hardcoded values.

The .env must be located inside the srcs folder:  vim srcs/.env

What must be inside .env? For the mandatory part, we need, for example: DOMAIN_NAME
										                                MYSQL_DATABASE
										  				                MYSQL_USER
										                                WP_TITLE
										                                WP_ADMIN_USER
										                                WP_ADMIN_EMAIL
										                                WP_USER
										                                WP_USER_EMAIL

How Does .env Work? How does, for example, WordPress know the value of MYSQL_USER?

Inside .env, we define the variable MYSQL_USER=wp_user.
Inside docker-compose.yml we have: 		services:
  										  wordpress:
                                            env_file:
                                              - .env

Docker Compose automatically reads the .env file and loads its variables.															
When the container starts, Docker Compose exports these variables into the container environment.
Inside the container echo $MYSQL_USER returns wp_user.

Why Is This Useful? Suppose we change MYSQL_USER=wp_user to MYSQL_USER=my_new_user.
No Dockerfile, script or configuration file needs to be modified.
After rebuilding the containers (docker compose up --build) the new value is automatically propagated 
everywhere.

What should NOT be inside .env? 

Passwords should not be hardcoded inside source files. We avoid: MYSQL_PASSWORD=1234
																 WP_ADMIN_PASSWORD=1234

Inside Dockerfiles, shell scripts, wp-config.php and Git repository.
Instead, store sensitive values using Docker secrets: secrets/db_root_password.txt
													  secrets/db_password.txt
													  secrets/wp_admin_password.txt
													  secrets/wp_user_password.txt

and read them at runtime.

What Should NOT Be Hardcoded? Avoid writing configuration values directly inside Dockerfiles, setup scripts,
wp-config.php, nginx.conf and docker-compose.yml. For example, we avoid: MYSQL_DATABASE=wordpress
                                                                         MYSQL_USER=wp_user

inside scripts.
Instead, we read the values from environment variables.
Passwords should be stored in Docker secrets instead. Why? The subject says credentials, passwords and API keys 
should not be stored directly in the Git repository. The .env file is useful for configuration, but it is not 
ideal for secrets.

Why Use Environment Variables? Environment variables allow the same codebase to be reused with different configurations.
For example: DOMAIN_NAME=rmedeiro.42.fr can be used simultaneously by Docker Compose, NGINX, WordPress and SSL certificate 
generation without modifying any source file.
Changing the domain only requires editing one line.

Important Evaluation Note: The evaluation sheet explicitly states that credentials, API keys and environment variables must be 
configured during the evaluation. For this reason: srcs/.env and secrets/ should not be committed to the Git repository.
We have to add them to: srcs/.env and secrets/ and create them locally during evaluation.


==========================================================================================================================================================================

Create Docker Secrets

Docker Secrets are used to store sensitive information such as passwords outside of the source code.
Instead of hardcoding passwords directly inside we store them in dedicated files and make them available to containers 
at runtime.
This improves security and follows the recommendation of the Inception subject, which explicitly states that credentials and 
sensitive information should not be exposed in the repository.

If it does not already exists, we create the Secrets Directory:  mkdir -p secrets

We create the following secret files: echo "<DB_ROOT_PASSWORD>" > secrets/db_root_password.txt
						              echo "<DB_PASSWORD>" > secrets/db_password.txt
						              echo "<WP_ADMIN_PASSWORD>" > secrets/wp_admin_password.txt
						              echo "<WP_USER_PASSWORD>" > secrets/wp_user_password.txt

What each secret is for?

-> db_root_password.txt: Contains the password for the MariaDB root user account.
-> db_password.txt: Contains the password for the normal MariaDB user used by WordPress.
                    WordPress uses this account to connect to MariaDB, create tables, read data and write data.
-> wp_admin_password.txt: Contais the password for the WordPress administrator account.
-> wp_user_password.txt: Contains Password for the normal WordPress user.

How Do Docker Secrets Work? How does WordPress access the database password if it is not inside the code?

Example: 

secrets/db_password.txt is declared inside:  secrets:
                                               db_password:
                                                 file: ../secrets/db_password.txt

and attached to a service:  services:
                              wordpress:
                                secrets:
                                  - db_password

Inside the container Docker automatically creates /run/secrets/db_password .
The initialization script can then read it DB_PASSWORD=$(cat /run/secrets/db_password) without exposing 
the password anywhere in the source code.

Set Secure Permissions on Secrets:  chmod 600 secrets/*

What does chmod 600 mean? 6 - owner can read and write
						  0 - group has no permissions
                          0 - others have no permissions

So only the owner can read and edit the files.
In practice: -rw------- db_password.txt

Why Do We Use chmod 600? Secrets contain sensitive information. Restricting permissions prevents accidental 
exposure, unauthorized access and other users on the system reading passwords.
Even though Inception is developed inside a local VM, using restrictive permissions is considered good practice 
and reflects real-world server security practices.

==========================================================================================================================================================================

Create .gitignore

The .gitignore file tells Git which files and directories should not be tracked by the repository.
This is especially important for Inception because some files contain passwords, credentials, machine-specific 
configuration and local development data. These files should never be pushed to GitHub.

At the root of the project, we create the .gitignore: vim .gitignore and add: secrets/ and srcs/.env  .

Why Ignore secrets/? The secrets/ directory contains passwords. These files contain sensitive information and must 
never be committed to the repository. If they are pushed to GitHub, anyone with access to the repository can read 
the passwords. Ignoring this directory ensures that credentials remain local to the machine where the project is being 
executed.

Why Ignore srcs/.env? The .env file contains environment variables. Even if passwords are stored in Docker Secrets, the 
.env file may still contain machine-specific configuration such as DOMAIN_NAME, database names, usernames and email addresses.
Keeping the file local makes the project easier to configure on different machines.

Important Evaluation Note: The evaluation sheet explicitly states that credentials, API keys and environment variables
must be configured during the evaluation. For this reason, srcs/.env and secrets/ should not be present in the Git repository.
Instead, they should be created locally before running the project.

Recommended Practice: A solution is to provide an example file srcs/.env.example , which contains the expected variables without 
any sensitive values.

Example:

DOMAIN_NAME=<DOMAIN_NAME>
MYSQL_DATABASE=<MYSQL_DATABASE>
MYSQL_USER=<MYSQL_USER>
WP_ADMIN_USER=<WP_ADMIN_USER>
WP_ADMIN_EMAIL=<WP_ADMIN_EMAIL>
WP_USER=<WP_USER>
WP_USER_EMAIL=<WP_USER_EMAIL>

This allows other users or evaluators to understand which variables are required while still keeping the real .env private.

==========================================================================================================================================================================

MariaDB Service

The MariaDB container is the first service implemented in the Inception project. Its purpose is to provide a 
database server that will later be used by the WordPress container.
The MariaDB service is composed of three files:

								requirements/mariadb/
								├── Dockerfile
								├── conf/
								│   └── 50-server.cnf
								└── tools/
									└── init_mariadb.sh

# Dockerfile

The Dockerfile is responsible for building the MariaDB image. It starts from a Debian base image, installs MariaDB 
Server, copies the configuration files and defines the startup script executed when the container starts.

## FROM debian:bookworm

Uses Debian Bookworm as the base image. The Inception subject requires every service to be built from a Debian or Alpine image.
All subsequent instructions are executed inside this Debian environment.

## EXPOSE 3306

Documents that MariaDB listens on port 3306. It simply indicates which port the service uses internally.

## Install MariaDB Server

Updates the package lists and installs MariaDB Server. This package provides: - mariadbd
																			  - database engine
																			  - system tables
																			  - configuration files

Without it, the container would not be able to run MariaDB.

==========================================================================================================================================================================

Testing the MariaDB Docker Compose Configuration

Before building the container, we have to make sure if exists: srcs/docker-compose.yml
                                                               srcs/.env
                                                               secrets/db_root_password.txt
															   secrets/db_password.txt
															   srcs/requirements/mariadb/Dockerfile

Without a Dockerfile, Docker Compose cannot build the MariaDB image.

Validate the Docker Compose Configuration

Move to the srcs directory cd ~/inception/srcs and check the configuration: docker compose config
This command checks docker-compose, networks, services without creating containers, images or volumes.

Build the MariaDB Image: docker compose build mariadb
This command reads the Dockerfile, builds the image and does not start any container.
If the build succeeds, Docker will create a local image called mariadb.

Verify the Image - list all local images: docker images
Expected output: mariadb

Create and Start the Container - start the MariaDB service: docker compose up mariadb
Docker will create the network, the volume, the container and start the MariaDB service.

Verify Running Containers - Open a new terminal and execute: docker ps

Enter inside the Container:  docker exec -it mariadb sh

Verify Docker Secrets - Inside the container: ls /run/secrets
Expected output: db_root_password
                 db_password

Verify Docker Volume:  docker volume ls

Inspect the Volume - Display detailed volume information:  docker volume inspect mariadb
Expected output: /home/rmedeiro/data/mariadb

Verify the Host Directory - Check the actual files stored on the host: ls -la /home/rmedeiro/data/mariadb
Once MariaDB starts correctly, database files should begin appearing in this directory.
This confirms that data persistence is working correctly.

Stop the Service - Stop the running container: docker compose down
This removes containers and networks but keeps volumes and database data.




# The purpose of MariaDB server settings is to: - define a directory where MariaDB stores all databases, tables and internal files.
#                                                 Is mounted to a Docker volume for data persistence.
#                                               - configure the UNIX socket used by local MariaDB clients inside the container.
#                                                 Allows local communication without using the network stack.
#                                               - Listen on all available network interfaces. Allow connections from any IP. This is 
#                                                 required so that the WordPress container can connect to MariaDB through the Docker network.
#                                               - specify the default MariaDB TCP port used by MariaDB. WordPress will connect to this 
#                                                 port using mariadb:3306
#                                               - ensure MariaDB runs with the correct system user