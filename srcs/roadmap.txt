VM Setup

	Hostname: rmedeiro42
	Domain name: empty
	Root password: root
	Full Name: Ricardo Medeiros
	Username: rmedeiro
	Password: 1234
	Partitions: Guided - use entire disk
	Partition scheme: All file in one partition
	Finish partitioning and write changes to disk
	Write the changes to disk? Yes
	Scan extra installation media? Yes. And it will show another answer box and click No
	Debian archive mirror: deb.debian.org
	Choose software to install: [*] Debian desktop environment
								[*] SSH server
								[*] standard system utilities	
	Install GRUB bootloader: Yes, and install in /dev/sda
	FInish the installation: Continue

==========================================================================================================================================================================

Debian Setup

	1) Install sudo: 
	
	   a) Switch to the root user:  su -

	   b) Update and upgrade the installed packages:  apt-get update && apt-get upgrade -y

	   c) Installs the sudo package, allowing users to execute commands with admnistrative privileges:  
	      apt-get install sudo -y

    2) Add User to the Sudo Group: 
	
	   a) Add user to the sudo group:  adduser rmedeiro sudo

	      Grants admnistrative privileges to the user through Debian sudo group.
	
	   b) Check that the user belongs to the sudo group:   getent group sudo

	   c) Apply the change rebooting the system:  sudo reboot

	3) Update system:  sudo apt update && sudo apt upgrade -y

	4) Install Vim: sudo apt install -y vim

	5) Go to sudoers file: sudo visudo
	   Edit the line below the root line adding username and the privileges: rmedeiro ALL=(ALL:ALL) ALL

	6) Install OpenSSH Server: 
	
	   a) Install OpenSSH server:  sudo apt install -y openssh-server

	      Allows remote connections to the virtual machine using SSH

	   b) Enable SSH at boot:  sudo systemctl enable ssh

	   c) Start the SSH service:  sudo systemctl start ssh

	   d) Check that SSH is running:  sudo systemctl status ssh

	      Expected status: active (running).
     
	7) Change SSH port 22 to 4242: 
	
	   a) Open the SSH configuration file:  sudo vim /etc/ssh/sshd_config

	   b) Find: #Port 22 and replace with Port 4242. Save and exit.

	      Changes the SSH listenning port 22 to 4242.

	   c) Restart SSH:  sudo systemctl restart ssh

	   d) Check:  sudo systemctl status ssh (check)

	8) Configure VS COde Remote SSH:  

	   a) Install the Remote - SSH extension in VS Code.

       b) Connect to the VM: Open VS Code and press Ctrl + Shift + P .

	   c) Select: Remote-SSH: Connect to Host... and choose Add New SSH Host...

	   d) Enter: ssh -p 4242 rmedeiro@<VM_IP>  (Example: ssh -p 4242 rmedeiro@192.168.1.120)

          To obtain the VM IP: hostname -I .

	   e) Select the SSH config file, connect to the host and enter the VM password when prompted.

    9) Install aplications: sudo apt install -y git curl wget tree zsh make gcc build-essential ca-certificates gnupg lsb-release apt-transport-https
                            chsh -s $(which zsh)  (default zsh)
                            OhMyZsh: sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"


==========================================================================================================================================================================

Docker Engine installation

	> Update System: sudo apt-get update && sudo apt-get upgrade -y

	> Install Required Depedencies: sudo apt-get install ca-certificates curl gnupg -y

      Installs the packages required to securely download and verify Docker packages: -> ca-certificates: HTTPS certificate validation.
																					  -> curl: Download files from the internet.
																					  -> gnupg: Verify package signatures using GPG keys.

	> Create Keyrings Directory: sudo install -m 0755 -d /etc/apt/keyrings

      Creates the directory where trusted repository keys will be stored.

	> Add official Docker key: curl -fsSL https://download.docker.com/linux/debian/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

      Downloads Docker's official signing key and converts it to the binary GPG format required by APT.

	> Set Key Permissions: sudo chmod a+r /etc/apt/keyrings/docker.gpg

	  Allows the APT package manager to read the Docker GPG key.

	> Add Docker repository: echo "deb [arch="$(dpkg --print-architecture)" signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian \
							"$(. /etc/os-release && echo "$VERSION_CODENAME")" stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

	  Adds Docker's official repository to the APT sources list.

	> Update packages: sudo apt-get update

	> Install Docker Engine: sudo apt-get install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

      Installs: -> docker-ce: Docker Engine.
				-> docker-ce-cli: Docker command-line interface.
				-> containerd.io: Container runtime.
				-> docker-buildx-plugin: Advanced image building support.
				-> docker-compose-plugin: Docker Compose v2 (docker compose).

	> Start Docker Service: sudo systemctl start docker

	  Starts the Docker daemon immediately.

	> Enable Docker at Boot: sudo systemctl enable docker

	  Ensures Docker starts automatically every time the system boots.

	> Add User to Docker Group: sudo gpasswd -a $USER docker

	  Allows the current user to run Docker commands without using sudo.

	> Apply Group Changes: newgrp docker

	  Applies the new group membership without requiring a logout or reboot.

	> Verify Docker Installation: docker run hello-world

	  Downloads and runs Docker's test container. Expected output: Hello from Docker!
      If this message appears, Docker is installed correctly.

	> Verify Docker Compose: docker compose version

      Checks that Docker Compose v2 is installed and available.

	> Check Docker Service Status: systemctl status docker

	  Displays the current state of the Docker daemon.
      Expected status: active (running)

    > Display Docker Information: docker info

	  Displays detailed information about: - Docker version
										   - Storage driver
										   - Available networks
										   - Available volumes
										   - Docker root directory
										   - Running containers

	  This command is useful to confirm that the Docker daemon is fully operational.

==========================================================================================================================================================================


Enable Access to website via username.42.fr

	> Go to: sudo vim /etc/hosts

	  We gonna see '127.0.0.1 localhost'
	               '127.0.1.1 debian'

	  And, add '127.0.0.1 rmedeiro.42.fr'